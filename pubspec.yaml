name: turnos_78
description: A new Flutter project

# Version of your application
version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Actualizar las versiones de googleapis y googleapis_auth
  googleapis: ^14.0.0
  googleapis_auth: ^1.4.1

  # Otras dependencias
  cupertino_icons: ^1.0.2
  intl: ^0.20.2
  shared_preferences: ^2.5.3
  # flutter_bluetooth_serial:
  #   path: plugins/flutter_bluetooth_serial
  file_picker: ^6.2.1
  firebase_storage: ^12.0.0
  video_player: ^2.8.6
  # blue_thermal_printer:
  #   path: plugins/blue_thermal_printer
  pdf: ^3.11.3
  printing: ^5.13.1
  image_picker: ^1.1.2
  network_info_plus: ^6.1.4
  web_socket_channel: ^3.0.3
  audioplayers: ^6.5.0
  permission_handler: ^12.0.1
  firebase_core: ^3.0.0
  cloud_firestore: ^5.6.12
  
  flutter_blue_plus: ^1.16.5
  excel: ^4.0.6
  blue_thermal_printer: ^1.2.3
  # flutter_bluetooth_serial: ^0.4.0  # Removed temporarily; using native RFCOMM channel in MainActivity
  

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/sounds/

  # Uncomment and configure fonts if needed
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700

dependency_overrides:
  firebase_core_web: ^3.0.0